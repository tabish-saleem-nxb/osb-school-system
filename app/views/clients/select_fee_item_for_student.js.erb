<% if @fee_items.present? %>
    $('table#invoice_grid_fields').find('.dynamic_fee_item').remove();
    $('#invoice_grid_fields > tbody > tr:first').after('<%= escape_javascript render("invoice_line_item_fields") %>');
<% end %>
