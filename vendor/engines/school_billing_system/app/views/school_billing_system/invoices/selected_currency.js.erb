window.currency_symbol = "<%= @currency.unit %>"
$("#invoice_sub_total_lbl, #invoice_total_lbl, .tax_amount, .invoice_arrears").formatCurrency({symbol: window.currency_symbol})
$('#discount_type').find('option:last').val("<%= @currency.unit %>").html("<%= @currency.unit %>").trigger("liszt:updated");
