tr
  th.align_center.not-sortable  style='visibility:hidden' = check_box_tag 'select_all'
  th.align_center.not-sortable = t('views.term_rules.name')
  th.align_center.not-sortable = t('views.term_rules.description')
  th.align_center.not-sortable = t('views.term_rules.frequency')
  th class="align_center not-sortable" = t '.actions', :default => t("helpers.actions")
