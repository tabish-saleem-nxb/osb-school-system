- model_class = TermRule
.term_rules
  .content_top
    .primary_heading
      h1
        = t '.title', :default => model_class.model_name.human.pluralize.titleize
    .primary_btn
      = link_to (content_tag(:span, t("header.links.term_rules.new"))), new_term_rule_path, :class => 'btn_large'

  .content_middle
    .grid_control
      .grid_table
        table.table.table_listing.client_listing cellpadding="0" cellspacing="0" class=("#{ I18n.locale}")
          colgroup
            col.one
            col.two
            col.three
            col.four
            col.five
            col.six
            col.seven
          thead#term_rule_head
            = render partial: 'term_rule_header'
          tbody#term_rule_body
            = render partial: 'term_rule_body'

          tfoot#paging_links
            /= render 'application_partials/paging'
      .content_bottom
        = link_to (content_tag(:span, t("header.links.term_rules.new"))), new_term_rule_path, :class => 'btn_large'
